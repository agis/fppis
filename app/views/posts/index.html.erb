<%# OPTIMIZE: Reduce font size of title labels (same as titles) ASK COREY %>
<div class="ninecol" id="left">
  <%= render 'ruler' %>
  <%= render @posts %>
  <%= render 'pagination' %>
</div>

<div class="threecol last" id="right">
  <%= render 'posts_menu' %>
</div>